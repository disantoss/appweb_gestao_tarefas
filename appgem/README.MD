# Task Manager Pro - Analytics Edition

Aplicativo completo em Python + Streamlit focado no gerenciamento de tarefas e produtividade para profissionais de Dados.

## 🚀 Como Rodar Localmente (Localhost)

1. **Abra o terminal** na pasta onde você salvou os arquivos.
2. **Crie um ambiente virtual** (opcional, mas recomendado):
   ```bash
   python -m venv venv
   # No Windows:
   venv\Scripts\activate
   # No Mac/Linux:
   source venv/bin/activate